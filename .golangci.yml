version: "2"

linters:
  default: standard
  enable:
    - misspell
  settings:
    errcheck:
      exclude-functions:
        - io.WriteString
        - (net/http.ResponseWriter).Write
  exclusions:
    presets:
      - std-error-handling

formatters:
  enable:
    - gofmt
    - goimports
